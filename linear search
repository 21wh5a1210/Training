#include <iostream>

using namespace std;

int
main ()
{
  int arr[5];
  int key = 7, flag = 0;
  for (int i = 0; i <= 5; i++)
    {
      cin >> arr[i];
      if (key == arr[i])
	{
	  flag = 1;
	  break;

	}
    }
  if (flag == 1)
    {
      cout << "True";
    }
  else
    {
      cout << "False";
    }

  return 0;
}
